<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="ROBOTS" content="NOODP" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <title><%= title %></title>
  <%= stylesheet_link_tag :application, media: "all" %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= body_class %>">
  <div class="container">
    <div class="row">
      <div class="col-md-6">
        <% if signed_in? %>
          <table class="table">
            <tr>
              <th> 
                <%= "Signed in as " + current_user.email %>
              </th>
              <th>
                <%= button_to "Sign out", sign_out_path, method: :delete %>    
              </th>
            </tr>
          </table>
        <% else %>
          <%= link_to "Sign in to Survey Maker", sign_in_path %>
        <% end %>
        <%= render "flashes" -%>
        <%= yield %>
      </div>
    </div>
  </div>
  <%= render "javascript" %>
  <%= render "css_overrides" %>
</body>
</html>
